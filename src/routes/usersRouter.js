import { Router } from "express";

const usersRouter = Router();

//CRUD LIMITADO USER SOLO C
//RETURN USER BY ID 
usersRouter.get("/:id", );
//DELETE USER BY ID
usersRouter.delete("/:id", );

export default usersRouter;